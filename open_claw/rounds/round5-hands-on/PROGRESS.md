# PROGRESS - Round 5: Hands-on Phase
**Người dùng**: ĐươngDaniel
**Mục tiêu**: Thử nghiệm thực tế pi-mono: tạo conversation, tool execution, session ops.
**Start**: (chưa bắt đầu)
**Status**: ⏳ Planning

---

## 📋 Steps Logged

### Step 1: Tạo thư mục Round 5 (YYYY-MM-DD HH:MM)
- Tạo `round5-hands-on/` với `notes/`, `artifacts/`.
- Tạo `README.md`, `checklist.md`.

---

## 🎯 Planned Actions

1. **Setup**:
   - Kiểm tra Node.js, npm.
   - `npm install` trong pi-mono packages/coding-agent.
   - Build (nếu cần).
   - Test `pi --version`.
   - `/login` hoặc set API key.
   - `pi --list-models`.

2. **Conversation**:
   - Chạy `pi`.
   - Gửi "Xin chào".
   - Xem session file tại `~/.pi/agent/sessions/--<path>--/`.
   - Parse JSONL, xem entries.

3. **Tool execution**:
   - Dùng `read`, `bash`, `write`, `edit`.
   - Ghi lại args và results.

4. **Session ops**:
   - `/new`, `/resume`, `/tree`, `/fork`, `/compact`.

5. **Custom tool** (nếu có thời gian):
   - Tạo extension với tool `echo`.
   - Load và test.

6. **Report**:
   - Tổng hợp notes.
   - Lưu artifacts.

---

## 📝 Notes (sẽ điền sau)

[Chưa có notes. Sẽ tạo `notes/01-setup.md`, `02-conversation.md`, `03-tool-execution.md`, `04-session-ops.md`, `05-custom-tool.md`, `06-lessons.md` khi thực hiện.]

---

**Tiến độ**: 0% (chưa bắt đầu)
